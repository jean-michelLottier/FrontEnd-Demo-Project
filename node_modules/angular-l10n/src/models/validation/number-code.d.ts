import { LocaleService } from '../../services/locale.service';
export declare abstract class NumberCode {
    protected locale: LocaleService;
    protected readonly numberCodes: string[];
    constructor(locale: LocaleService);
    abstract parse(s: string): number;
    abstract getRegExp(digits: string): RegExp;
    protected toChar(pattern: string): string;
    protected toUnicode(c: string): string;
    private toHex(value);
}
